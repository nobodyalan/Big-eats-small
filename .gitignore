# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 模型权重 / 训练产物(cache 里的 .pt 很大, 不入库) ──
cache/
models/

# ── 运行日志（配置文件保留在 configs/，日志不入库） ─────
logs/

# ── 大数据集 / 训练数据(超过 GitHub 100MB 限制, 不入库) ──
data/

# ── 评测输出 ────────────────────────────────────────────
eval_results/

# ── 本地工具 / 编辑器 ───────────────────────────────────
.claude/
.vscode/
.idea/

# ── 文档 PDF(本地参考, 不入库) ───────────────────────────
big-small-approaches.pdf

# ── Jupyter ─────────────────────────────────────────────
.ipynb_checkpoints/

# ── 系统 ────────────────────────────────────────────────
Thumbs.db
.DS_Store
